# Commit 1/9 — Verificar que las paginas de Breeze quedaron en minuscula

**Rama de trabajo:** `frontend`  
**Accion:** verificacion / ajuste manual (no hay codigo para copiar)

## Por que (contexto)

Styveen ya debio mover `resources/js/Pages/` a `resources/js/pages/`
al instalar Breeze (su commit 1). Antes de seguir, confirma que:

1. Existe `resources/js/pages/Auth/Login.vue` y
   `resources/js/pages/Auth/Register.vue` (minuscula).
2. NO existe una carpeta `resources/js/Pages/` (mayuscula) — si
   todavia existe, es que algo quedo duplicado, muevela y borrala.
3. Abre `resources/js/app.js` y confirma que el glob sigue en
   minuscula: `import.meta.glob('./pages/**/*.vue', ...)`.

Si algo no cuadra, arreglalo ahora — todo lo que sigue depende de que
esto este bien, si no las paginas nuevas no van a cargar.


## Codigo


## Comandos a ejecutar

```powershell
# Solo verificacion, si todo esta bien no hay nada que commitear.
# Si tuviste que mover algo:
git add -A
git commit -m "fix(ui): normalizar rutas de paginas de Breeze a minuscula"
```

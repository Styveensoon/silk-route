# Commit 7/8 — Indice compuesto para optimizar busqueda de servicios

**Rama de trabajo:** `database`  
**Accion:** crear archivo

## Por que (contexto)

El listado principal filtra por categoria y por status = 'active'
(lo veras en el repositorio de Styveen). Un indice compuesto sobre esas
dos columnas acelera esa consulta, que es la mas frecuente del sistema
segun el requerimiento no funcional de rendimiento del Producto 01
(< 2s en operaciones CRUD).


## Codigo


**Archivo:** `database/migrations/2026_08_02_100003_add_index_to_services_table.php`

```php
<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::table('services', function (Blueprint $table) {
            $table->index(['category_id', 'status'], 'services_category_status_index');
        });
    }

    public function down(): void
    {
        Schema::table('services', function (Blueprint $table) {
            $table->dropIndex('services_category_status_index');
        });
    }
};
```


## Comandos a ejecutar

```powershell
git add database/migrations/2026_08_02_100003_add_index_to_services_table.php
git commit -m "feat(db): agregar indice compuesto category_id+status a services"
```

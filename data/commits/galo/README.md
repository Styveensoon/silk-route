# Galo — Base de datos (rama `database`)

Antes de empezar:
```powershell
git checkout main
git pull origin main
git checkout database
git merge main
```

Ejecuta 1.md a 8.md en orden. Al terminar el 8:
```powershell
git push origin database
```
Y abres el Pull Request `database -> main` en GitHub.

# Commit 3/8 — Componente ServiceCard

**Rama de trabajo:** `frontend`  
**Accion:** crear archivo

## Por que (contexto)

Componente reutilizable para mostrar un servicio en tarjeta. Lo usa la
pagina de listado (commit 4). Recibe el objeto `service` tal cual lo
manda el backend (con `user` y `category` ya incluidos gracias al
`with()` del repositorio de Styveen).


## Codigo


**Archivo:** `resources/js/Components/ServiceCard.vue`

```vue
<template>
  <div class="border border-gray-200 rounded-lg p-4 bg-white shadow-sm hover:shadow-md transition">
    <span class="text-xs font-medium text-indigo-600 uppercase">{{ service.category?.name }}</span>
    <h3 class="text-lg font-semibold text-gray-900 mt-1">{{ service.title }}</h3>
    <p class="text-sm text-gray-600 mt-1 line-clamp-2">{{ service.description }}</p>
    <div class="flex items-center justify-between mt-4">
      <span class="text-sm text-gray-500">{{ service.user?.name }}</span>
      <span class="font-bold text-gray-900">${{ service.price }}</span>
    </div>
  </div>
</template>

<script setup>
defineProps({
  service: { type: Object, required: true },
});
</script>
```


## Comandos a ejecutar

```powershell
git add resources/js/Components/ServiceCard.vue
git commit -m "feat(ui): crear componente ServiceCard"
```

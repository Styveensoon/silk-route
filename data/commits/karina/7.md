# Commit 7/8 — Ajustes de estilo responsivo

**Rama de trabajo:** `frontend`  
**Accion:** modificar 2 archivos (cambios puntuales, no todo el archivo)

## Por que (contexto)

Ajustes puntuales de responsive/consistencia visual, no un rediseño.
Por eso este commit es de tipo `style` (no cambia comportamiento, solo
apariencia), y por eso se describen como cambios de linea especifica en
vez de reemplazar los archivos completos.


## Codigo


**Archivo:** `resources/js/pages/Services/Index.vue`

```text
Busca esta linea:

  <h1 class="text-2xl font-bold text-gray-900 mb-6">Servicios disponibles</h1>

Y cambiala por:

  <h1 class="text-2xl sm:text-3xl font-bold text-gray-900 mb-6">Servicios disponibles</h1>

Busca esta linea:

  <div v-else class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">

Y cambiala por (agrega gap-6 y items-stretch):

  <div v-else class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 items-stretch">
```


**Archivo:** `resources/js/Components/ServiceCard.vue`

```text
Busca esta linea:

  <div class="border border-gray-200 rounded-lg p-4 bg-white shadow-sm hover:shadow-md transition">

Y cambiala por (se agrega h-full flex flex-col para que las tarjetas
queden parejas en el grid):

  <div class="border border-gray-200 rounded-lg p-4 bg-white shadow-sm hover:shadow-md transition h-full flex flex-col">
```


## Comandos a ejecutar

```powershell
git add resources/js/pages/Services/Index.vue resources/js/Components/ServiceCard.vue
git commit -m "style(ui): ajustar espaciado y tamanos responsivos del listado de servicios"
```

# Commit 6/8 — Validacion: StoreServiceRequest

**Rama de trabajo:** `backend`  
**Accion:** crear archivo

## Por que (contexto)

Form Request separado del controlador, para que las reglas de validacion
no vivan mezcladas con la logica HTTP. `exists:categories,id` evita que
llegue un category_id que no existe.


## Codigo


**Archivo:** `app/Http/Requests/StoreServiceRequest.php`

```php
<?php

namespace App\Http\Requests;

use Illuminate\Foundation\Http\FormRequest;

class StoreServiceRequest extends FormRequest
{
    public function authorize(): bool
    {
        return true;
    }

    public function rules(): array
    {
        return [
            'title' => ['required', 'string', 'max:120'],
            'description' => ['required', 'string', 'max:2000'],
            'price' => ['required', 'numeric', 'min:0'],
            'category_id' => ['required', 'exists:categories,id'],
        ];
    }
}
```


## Comandos a ejecutar

```powershell
git add app/Http/Requests/StoreServiceRequest.php
git commit -m "feat(services): crear StoreServiceRequest con reglas de validacion"
```
